# Assets

Place README/docs media here.

- `demo.gif` — short screen capture for the README "See it running" section.
  Suggested flow: register → login → create account → create budget → create
  transaction → `422` when it exceeds the budget limit. Keep it under ~10s and ~5 MB.
- Swagger/UI screenshots, diagram exports, etc.
